name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: List files for debugging (временно)
        run: ls -R

      - name: Generate rp.properties from template
        env:
          LOGIN: ${{ secrets.LOGIN }}
          PASSWORD: ${{ secrets.PASSWORD }}
        run: |
          echo "Generating rp.properties..."
          envsubst < src/main/resources/rp.properties.template > src/test/resources/rp.properties


      - name: Build with Maven
        run: mvn clean install

      - name: Run tests
        run: mvn test
